Tải node module trước khi chạy dự án : npm install

Nếu gặp lỗi cài đặt/EPERM/thiếu react-refresh: Chạy "npm cache clean --force" rồi chạy "npm install react-refresh --save-dev; npm install"

sau đó khởi động zalo mini app